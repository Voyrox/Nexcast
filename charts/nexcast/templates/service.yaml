apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexcast.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "nexcast.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
{{ include "nexcast.selectorLabels" . | indent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
