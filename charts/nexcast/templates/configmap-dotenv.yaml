{{- if .Values.dotenv.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexcast.fullname" . }}-dotenv
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "nexcast.labels" . | indent 4 }}
data:
  .env: |
{{ .Values.dotenv.content | indent 4 }}
{{- end -}}
