version: "3.9"

services:
  nextcast-api:
    build:
      context: ../example
      dockerfile: Dockerfile
    container_name: nextcast-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
