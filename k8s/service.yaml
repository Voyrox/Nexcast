apiVersion: v1
kind: Service
metadata:
  name: nextcast-api
  namespace: default
  labels:
    app: nextcast-api
spec:
  selector:
    app: nextcast-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
